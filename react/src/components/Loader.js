import CircularProgress from '@mui/material/CircularProgress';

const Loader = () => {
  return <div style={{textAlign: 'center', padding: '1rem'}}><CircularProgress /></div>;
};

export default Loader;
